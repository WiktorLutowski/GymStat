<UserControl
    x:Class="GymStat.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:GymStat.ViewModels"
    d:Background="White"
    d:DataContext="{d:DesignInstance vm:HomeViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.Resources>
                <ControlTemplate x:Key="Triangle">
                    <Polygon Points="0, 24  40, 0  40, 48">
                        <Polygon.Fill>
                            <SolidColorBrush x:Name="PolygonColor" Color="#5e5e5e" />
                        </Polygon.Fill>
                    </Polygon>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>

                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="PolygonColor"
                                        Storyboard.TargetProperty="Color"
                                        To="Black"
                                        Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>

                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="PolygonColor"
                                        Storyboard.TargetProperty="Color"
                                        Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Control
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Template="{StaticResource Triangle}" />

            <TextBlock
                Grid.Column="1"
                Margin="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="40"
                Text="11.05.2026" />

            <Control
                Grid.Column="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                RenderTransformOrigin="0.5,0.5"
                Template="{StaticResource Triangle}">
                <Control.RenderTransform>
                    <RotateTransform Angle="180" />
                </Control.RenderTransform>
            </Control>
        </Grid>


    </Grid>
</UserControl>
