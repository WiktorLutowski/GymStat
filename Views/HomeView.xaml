<UserControl
    x:Class="GymStat.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:GymStat.ViewModels"
    d:Background="White"
    d:DataContext="{d:DesignInstance vm:HomeViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Grid Margin="0,0,0,20">
            <Grid.Resources>
                <ControlTemplate x:Key="Triangle">
                    <Polygon Points="0, 24  40, 0  40, 48">
                        <Polygon.Fill>
                            <SolidColorBrush x:Name="PolygonColor" Color="#5e5e5e" />
                        </Polygon.Fill>
                    </Polygon>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>

                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="PolygonColor"
                                        Storyboard.TargetProperty="Color"
                                        To="Black"
                                        Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>

                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="PolygonColor"
                                        Storyboard.TargetProperty="Color"
                                        Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Control
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Template="{StaticResource Triangle}" />

            <TextBlock
                Grid.Column="1"
                Margin="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="40"
                Text="11.05.2026" />

            <Control
                Grid.Column="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                RenderTransformOrigin="0.5,0.5"
                Template="{StaticResource Triangle}">
                <Control.RenderTransform>
                    <RotateTransform Angle="180" />
                </Control.RenderTransform>
            </Control>
        </Grid>

        <ItemsControl Grid.Row="1">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <Image Width="200" Source="/Images/squats.jpg" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="10"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="30"
                            Text="Przysiad z ciezarem"
                            TextWrapping="Wrap" />
                        <DataGrid Grid.Column="2">
                            <DataGrid.Columns>
                                <DataGridTextColumn Width="0.5*" Header="Seria" />
                                <DataGridTextColumn Width="*" Header="Ciężar" />
                                <DataGridTextColumn Width="*" Header="Powtórzenia" />
                            </DataGrid.Columns>
                        </DataGrid>

                        <Separator Grid.Row="2" Grid.ColumnSpan="3" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Button
            Grid.Row="2"
            Margin="10"
            Padding="10"
            HorizontalAlignment="Center"
            Content="Dodaj ćwiczenie"
            FontSize="30" />
    </Grid>
</UserControl>
