<UserControl
    x:Class="GymStat.Views.HomeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:GymStat.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:GymStat.ViewModels"
    d:Background="White"
    d:DataContext="{d:DesignInstance vm:HomeViewModel}"
    d:DesignHeight="900"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <conv:DateOnlyToStringConverter x:Key="DateOnlyToStringConverter" />
        <conv:ImageNameToPathConverter x:Key="ImageNameToPathConverter" />
        <conv:IndexToSeriesConverter x:Key="IndexToSeriesConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="10"
            HorizontalAlignment="Center"
            FontSize="30"
            FontWeight="Bold"
            Text="Ćwiczenia wykonane danym dniu" />

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.Resources>
                <ControlTemplate x:Key="Triangle">
                    <Polygon Points="0, 24  40, 0  40, 48">
                        <Polygon.Fill>
                            <SolidColorBrush x:Name="PolygonColor" Color="#5e5e5e" />
                        </Polygon.Fill>
                    </Polygon>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>

                        <EventTrigger RoutedEvent="MouseEnter">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="PolygonColor"
                                        Storyboard.TargetProperty="Color"
                                        To="Black"
                                        Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>

                        <EventTrigger RoutedEvent="MouseLeave">
                            <BeginStoryboard>
                                <Storyboard>
                                    <ColorAnimation
                                        Storyboard.TargetName="PolygonColor"
                                        Storyboard.TargetProperty="Color"
                                        Duration="0:0:0.2" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Grid.Resources>

            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Button
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Command="{Binding PreviousDateCommand}"
                Template="{StaticResource Triangle}" />

            <TextBlock
                Grid.Column="1"
                Margin="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="40"
                Text="{Binding CurrentSelectedDate, Converter={StaticResource DateOnlyToStringConverter}}" />

            <Button
                Grid.Column="2"
                HorizontalAlignment="Left"
                VerticalAlignment="Center"
                Command="{Binding NextDateCommand}"
                RenderTransformOrigin="0.5,0.5"
                Template="{StaticResource Triangle}">
                <Button.RenderTransform>
                    <RotateTransform Angle="180" />
                </Button.RenderTransform>
            </Button>
        </Grid>

        <Separator
            Grid.Row="2"
            VerticalAlignment="Top"
            BorderBrush="Black"
            BorderThickness="2" />

        <ScrollViewer
            Grid.Row="2"
            Margin="10"
            VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding ResultsFromSelectedDate}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto" />
                                <RowDefinition Height="auto" />
                            </Grid.RowDefinitions>

                            <Image Width="200" Source="{Binding Exercise.ExerciseImageFileName, Converter={StaticResource ImageNameToPathConverter}}" />

                            <TextBlock
                                Grid.Column="1"
                                Margin="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontSize="30"
                                Text="{Binding Exercise.ExerciseName}"
                                TextWrapping="Wrap" />

                            <DataGrid
                                Grid.Column="2"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                AlternationCount="1000"
                                AutoGenerateColumns="False"
                                CanUserAddRows="False"
                                CanUserDeleteRows="False"
                                IsReadOnly="True"
                                ItemsSource="{Binding Sets}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn
                                        Width="0.5*"
                                        Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=AlternationIndex, Converter={StaticResource IndexToSeriesConverter}}"
                                        Header="Seria" />

                                    <DataGridTextColumn
                                        Width="*"
                                        Binding="{Binding Weight}"
                                        Header="Ciężar" />

                                    <DataGridTextColumn
                                        Width="*"
                                        Binding="{Binding Repetitions}"
                                        Header="Powtórzenia" />
                                </DataGrid.Columns>
                            </DataGrid>

                            <Separator
                                Grid.Row="1"
                                Grid.ColumnSpan="3"
                                BorderBrush="Black"
                                BorderThickness="2" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button
            Grid.Row="3"
            Margin="10"
            Padding="10"
            HorizontalAlignment="Center"
            Content="Dodaj ćwiczenie"
            FontSize="30" />
    </Grid>
</UserControl>
